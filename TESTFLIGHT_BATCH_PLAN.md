# SuperCube TestFlight 分批测试计划

更新时间：2026-07-08

## 原则

先让真机稳定打开，再逐步放开玩法、离线兜底和联网对战。每一批只验证一层能力，避免多个风险同时上线后难以定位问题。

## Batch 0.1：启动验证包

- 版本：`1.0`
- Build：`3`
- 目标：确认真机可以安装、启动、加载线上 SuperCube 页面，并在弱网或失败时给出明确提示。
- 不包含：
  - 新玩法；
  - 新服务器；
  - 正式联网对战；
  - 离线完整 Web 包。

### 测试重点

- 首次安装后能打开 App；
- 真机 5G / Wi-Fi 下首屏能出现游戏画面；
- 加载慢时能看到加载状态；
- 加载失败或超时时能看到错误和“重新加载”按钮；
- 后台切回前台不崩溃；
- 旋转、刘海屏、安全区没有明显遮挡；
- 大厅、单人对战、教学、工坊至少能进入一次。

### 判定

- 通过：真机可稳定打开，基础入口可进入，失败时有提示。
- 不通过：仍然长期黑屏、无提示、崩溃，或线上页面在常用网络下无法稳定加载。

## Batch 0.2：离线兜底包

- 目标：在 App 内打包一份可用的 Web 资源或轻量兜底页。
- 判定：即使线上入口不可达，App 也不应停在纯黑或无限加载状态。

## Batch 0.3：Web 前端部署稳定包

- 目标：固定 Web 前端独立部署源，不再依赖 iOS-only `main`。
- 建议：
  - 使用 `web-production` 分支或独立 `SuperCube-Web` 仓库；
  - Vercel 只监听 Web 前端源；
  - 隐私政策 HTML 部署为公开 URL。

## Batch 0.4：联网对战内测包

- 目标：接入独立 WebSocket 对战服务器。
- 服务器：
  - Node + TypeScript + `ws`；
  - `/healthz` 健康检查；
  - 协议版本校验；
  - 匹配队列；
  - 房间生命周期；
  - 断线和超时处理；
  - 基础日志。

## Batch 0.5：外部测试包

- 目标：邀请外部测试者。
- 前置：
  - 隐私政策公开 URL 已填写；
  - App Privacy 已填写；
  - TestFlight beta 信息完整；
  - 线上入口和服务器稳定；
  - 已有一轮真机回归记录。
