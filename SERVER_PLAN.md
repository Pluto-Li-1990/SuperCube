# SuperCube 服务器与上线部署方案

更新时间：2026-07-08

## 当前结论

当前 iOS App 是一个原生 WKWebView 壳，加载线上网页：

`https://super-cube-rho.vercel.app`

这意味着首屏能否打开，主要取决于线上网页前端是否稳定可访问。联网对战服务器是另一层：它只在玩家进入在线对战、匹配、建房、同步回合时需要。

今天本地检查到该 Vercel 地址在 20 秒内返回 `HTTP 200`，但 10 秒请求曾出现超时。用户手机出现“黑屏加载”时，优先怀疑的是线上网页加载链路不稳定，而不是 WebSocket 对战服务器本身。

## 风险

1. `main` 现在已经切换为 iOS 原生工程，不再包含旧 Web 源码。
2. Vercel 当前线上页面仍能访问，说明旧部署还在，但未来如果 Vercel 继续从当前 `main` 自动部署，可能会因为缺少 Web 前端源码而破坏线上入口。
3. iOS 当前没有离线 Web 资源兜底，所以一旦线上网页慢、不可达或被重新部署坏，App 首屏就会停在加载状态。
4. Vercel 适合托管静态网页前端，但不适合作为长期运行的权威 WebSocket 对战服务器。

## 建议架构

### 1. 前端网页

保留一个专门的 Web 前端部署源，不要再依赖 iOS-only 的 `main`。

建议二选一：

- 建立 `web-production` 分支，保存可构建的网页游戏源码，并让 Vercel 只监听该分支；
- 或建立独立仓库 `SuperCube-Web`，专门用于网页前端和 Vercel 部署。

前端继续负责：

- 大厅、教学、工坊、单机玩法；
- 加载静态资源；
- 连接在线对战服务器；
- 展示在线匹配失败、服务器维护、版本不兼容等提示。

### 2. iOS App

iOS App 继续保持轻量：

- 默认加载稳定的线上前端 URL；
- 补一个离线兜底页面或打包版 Web 资源；
- 加载失败时显示明确错误和重试，不再纯黑屏；
- 上架前固定版本号、Bundle ID、隐私政策 URL 和 TestFlight 信息。

### 3. 实时对战服务器

在线对战应使用独立的 Node + TypeScript + `ws` 服务，部署到支持长连接的云服务器。

推荐优先级：

1. Render / Fly.io / Railway：最快能上线测试，支持常驻 Node 进程和 WebSocket；
2. DigitalOcean / Lightsail / 云服务器：控制力更强，维护成本更高；
3. 自建 VPS + Nginx + Node：适合正式稳定后迁移。

服务器职责：

- WebSocket 连接管理；
- 匹配队列；
- 房间创建与销毁；
- 回合锁步与消息校验；
- 协议版本检查；
- 断线、重连、超时处理；
- 基础限流与来源校验；
- 健康检查 `/healthz`；
- 结构化日志，方便排查真机问题。

### 4. 域名建议

建议最终拆成两个地址：

- `https://play.supercube.app`：网页前端；
- `wss://match.supercube.app`：实时对战服务器。

测试阶段可以先用平台临时域名，等 TestFlight 稳定后再绑定正式域名。

## 分阶段落地

### TestFlight 0.1

- 目标：让 App 可稳定打开，可测试单机、教学、工坊。
- 服务器：可以暂不启用正式对战服务器。
- 必做：
  - 固定 Vercel 前端部署源；
  - 补隐私政策公开 URL；
  - iOS 加载失败显示错误和重试；
  - 在线对战入口给出清楚的维护或匹配失败提示。

### TestFlight 0.2

- 目标：内部测试在线对战。
- 服务器：
  - 部署 Node + TypeScript + `ws` 服务；
  - 打开 `/healthz`；
  - 前端配置 `wss://...`；
  - 加入协议版本检查和房间超时清理。

### TestFlight 0.3

- 目标：外部测试更稳定。
- 服务器：
  - 加日志与简单监控；
  - 加连接数、房间数、错误数指标；
  - 加限流和来源校验；
  - 制定维护提示和降级文案。

## 近期最小行动清单

1. 从隔离目录或 Git 历史恢复 Web 前端源码，放到专门的 Web 分支或仓库。
2. 禁止 Vercel 从当前 iOS-only `main` 自动覆盖生产网页。
3. 部署隐私政策 HTML，并把公开 URL 填入 App Store Connect。
4. 决定实时对战服务器平台，优先 Render / Fly.io / Railway。
5. 恢复或重建之前的 `server/` Node + TypeScript + `ws` 服务，并接入线上前端。
6. iOS 端保留当前线上加载策略，同时继续补离线兜底，降低真机黑屏概率。
